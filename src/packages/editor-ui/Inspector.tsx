"use client";

import React from "react";

export function Inspector() {
    return (
        <div style={{ padding: 12, fontFamily: "system-ui", fontSize: 14 }}>
            <div style={{ fontWeight: 700, marginBottom: 8 }}>Inspector</div>
            <div style={{ color: "#6b7280" }}>ยังไม่ทำ selection/store รอบหน้า</div>
        </div>
    );
}
